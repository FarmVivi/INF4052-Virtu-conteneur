apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: td6-db-data
  name: td6-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
